name: "redshift_thesis"
version: "1.0.0"
config-version: 2

profile: "redshift_thesis"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

models:
  redshift_thesis:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
